# AI字幕生成(联动版) (AutoSubv3)

MoviePilot V2 插件，用于从视频音轨、内嵌字幕或外挂字幕中生成字幕，并可调用大模型翻译为中文。基于 autosub 系列插件继续整理，感谢原作者与后续贡献者。

## v3.5.60 更新

- 优化移动端任务卡片长文本展示，视频标题、来源和输出文件默认单行省略，减少长路径/长文件名占屏。
- 完整视频路径、完整失败原因和输出文件仍保留在详情展开区，兼顾手机端扫读和排错查看。
- 保持桌面任务列表布局不变，并通过真实 MoviePilot-Frontend 宿主截图回归验证。

## v3.5.59 更新

- 二次优化移动端任务页，任务移动卡片、工具栏筛选、批量底部操作栏和重跑 sheet 更适合单手触控。
- 长路径、失败原因、队列状态和批量选择信息在窄屏下分层展示，避免任务表被压窄后横向溢出。
- 已在真实 MoviePilot-Frontend 宿主验证 390px、430px、768px、1024px、1440px 视口和浅色/深色主题，桌面任务列表、工具栏和重跑弹窗布局保持不变。

## v3.5.58 更新

- 适配真实 MoviePilot-Frontend 宿主下的移动端任务页，工具栏、状态筛选、任务卡片和重跑弹窗在窄屏下不再横向溢出。
- 移动端样式限定在窄视口断点内，1024px 和 1440px 桌面视口已通过回归测试，保留桌面工具栏横向布局和任务信息密度。
- Playwright 真实宿主测试使用假任务数据和本地联邦 `remoteEntry.js`，覆盖浅色/深色主题、移动端、平板和桌面视口。

## v3.5.57 更新

- 优化设置页布局，将 API 配置前移到基础设置后，便于先完成大模型连接配置。
- “获取模型”和“测试模型”的结果改为在 API 配置区就近显示，不再跳到页面顶部。
- 设置页底部新增保存和关闭操作，长表单编辑完成后无需回到顶部保存。

## 功能特性

- 支持 OpenAI API 及兼容接口（硅基流动、DeepSeek 等）
- 支持 faster-whisper 多种模型（tiny/base/small/medium/large-v3/large-v3-turbo）
- 入库自动生成字幕并用 AI 翻译为中文
- 支持双语字幕或纯中文字幕输出模式
- 支持批量翻译 + 并发处理，速度更快
- 支持中文视频检测并跳过翻译
- 支持无声音视频自动跳过
- 支持联动字幕匹配插件，从字幕匹配页面单集或整季提交 AI 字幕生成任务并查看状态

## 字幕提取策略

优先级：**外挂字幕 > 内嵌字幕 > Whisper 语音识别**

| 策略 | 说明 |
|------|------|
| 仅英文 | 仅使用英文字幕，无则用 ASR |
| 英文优先 | 优先英文，无则用其他语言，再无则用 ASR |
| 原音优先 | 优先原音字幕，无则用英文，再无则用 ASR |

## 配置说明

### 基础配置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 启用插件 | 是否启用 | 否 |
| 媒体入库自动执行 | 监听入库事件自动执行 | 是 |
| 手动执行一次 | 保存后立即执行 | 否 |
| 发送通知 | 任务完成发送通知 | 否 |
| 文件最小大小（MB） | 小于该值不处理 | 10 |
| 字幕源语言偏好 | 字幕提取策略 | 英文优先 |
| 翻译为中文 | 是否翻译 | 是 |

### Whisper 设置

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 允许 ASR 生成字幕 | 是否用 Whisper 提取 | 是 |
| Whisper 模型 | 模型大小选择 | base |
| 使用代理下载模型 | 使用 MP 代理 | 是 |
| 自动检测语言 | 由 Whisper 识别语言 | 否 |

### 翻译参数

| 配置项 | 说明 | 默认值 |
|--------|------|--------|
| 上下文窗口大小 | 翻译时参考的上下文行数 | 5 |
| LLM 请求重试次数 | 失败重试次数 | 3 |
| 启用批量翻译 | 批量处理提升速度 | 是 |
| 每批翻译行数 | 建议 10-30 | 20 |
| 并发线程数 | 并发批次数 | 5 |

### API 配置

| 配置项 | 说明 | 示例 |
|--------|------|------|
| API URL | 接口地址 | https://api.siliconflow.cn |
| API 密钥 | API Key | sk-xxx |
| 模型 | 模型名称，可点击获取模型后选择 | inclusionAI/Ling-flash-2.0 |
| 使用代理服务器 | 通过代理访问 | 否 |
| 兼容模式 | 绕过 /v1 路径拼接 | 否 |
| 获取模型 | 从当前 API URL/API Key 查询可用模型列表 | - |
| 测试模型 | 用当前模型发送一次最小请求验证可用性 | - |

## 推荐配置

### 翻译模型

推荐使用性价比高的无推理模型：
- `inclusionAI/Ling-flash-2.0` - 速度快，便宜
- `inclusionAI/Ling-mini-2.0` - 更便宜

### Whisper 模型

| 模型 | 速度 | 准确度 | 适用场景 |
|------|------|--------|----------|
| tiny | 最快 | 一般 | 快速预览 |
| base | 快 | 较好 | 日常使用 |
| small | 中等 | 好 | 质量优先 |
| medium | 慢 | 很好 | 高质量 |
| large-v3 | 最慢 | 最好 | 极致质量 |
| large-v3-turbo | 较快 | 很好 | 平衡之选 |

## 注意事项

1. 首次使用 Whisper 会自动下载模型（约 1GB），建议开启代理
2. 批量翻译可大幅提升速度，但过大会影响翻译质量
3. 中文视频检测到后会自动跳过翻译（可关闭）
4. 无声音视频会自动跳过并记录
5. 任务队列机制保证并发安全

## 常见问题

**Q: 翻译结果为空或失败？**
A: 检查 API Key 是否正确，查看日志确认错误原因

**Q: Whisper 提取很慢？**
A: 使用更小的模型（tiny/base），或考虑 GPU 加速

**Q: 批量翻译失败率高？**
A: 降低 batch_size（建议 10-20），检查模型能力

## 版本历史

- v3.5.60: 优化移动端任务卡片长标题、来源和输出文件展示，默认单行省略，完整路径和失败原因保留在详情区，桌面任务列表布局不变
- v3.5.59: 二次优化真实 MoviePilot 前端宿主下的移动端任务页，任务卡片、批量操作栏和重跑弹窗更适合触控，并验证浅色/深色主题与桌面布局不受影响
- v3.5.58: 适配真实 MoviePilot 前端宿主下的移动端任务页，优化工具栏、状态筛选、任务卡片和重跑弹窗窄屏显示，并补充多视口回归测试以保持桌面布局不变
- v3.5.57: 优化设置页布局，API 模型获取和测试结果改为就近显示，底部新增保存操作
- v3.5.56: 字幕联动设置页新增获取模型和测试模型功能，补充模型配置接口测试
- v3.5.55: 后端分层拆分，前端任务页组件化，补充分层与 API 合同测试
- v3.5.54: 任务页状态和队列能力维护优化
- v3.5.51: 支持字幕匹配插件传入外部在线字幕作为本次任务的字幕覆盖源
- v3.5.46: 修复设置弹窗空白问题；翻译失败率超过 30% 时停止输出字幕文件
- v3.5.41: 改名为 AI字幕生成(联动版)，新增联动提交与任务状态 API，可在字幕匹配页面单集/整季调用并查看状态
- v3.5.40: 插件改名为 AI字幕生成，更新作者信息并合并到 ifsherlock 插件仓库
- v3.5.39: 放宽 python-dotenv 版本约束，兼容 MoviePilot 现有依赖
- v3.5.38: UI：API申请教程链接改为稳定仓库锚点
- v3.5.37: Whisper 耗时统计与分段警告；翻译统计修正；日志优化
- v3.5.36: Whisper 模型下载重试机制；日志格式统一
- v3.5.35: 批量翻译优化；JSON 解析增强
- v3.5.33-34: Whisper 进度日志优化
- v3.5.31: Ling-flash-2.0 测试通过；few-shot 格式优化
- v3.5.26: 中文视频检测时机优化
- v3.5.24: 中文视频不翻译开关修复
- v3.5.21: 无声音跳过功能
