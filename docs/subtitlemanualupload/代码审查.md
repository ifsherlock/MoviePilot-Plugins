## 2026-06-28 23:55

## 代码审查报告

### 审查目标
- 类型：本地结构审查
- 范围：`plugins.v2/subtitlemanualupload`
- 审查时间：2026-06-28 23:55

### 审查输入证据
- `plugins.v2/subtitlemanualupload/__init__.py`：7293 行，240 个函数/方法。
- 已拆模块：`online_subtitles/`、`timeline_fixer.py`、`tongwen.py`。
- 体量最大的 Python 文件：`__init__.py` 7293 行、`online_subtitles/common.py` 1766 行、`timeline_fixer.py` 1216 行。
- 前端体量：`src/components/AppPage.vue` 5419 行，`Config.vue` 833 行。
- 自动化检查：尝试运行 `python -m pytest tests/test_subtitlemanualupload_cache.py tests/test_subtitlemanualupload_online.py tests/test_timeline_fixer.py -q`，当前 Codex bundled Python 缺少 `pytest`，未执行成功。
- Git 状态：当前目录是 ZIP 展开的源码，不是 Git 仓库，无法基于 diff 审查。

### 问题清单

#### 🔴 严重问题
无源码级严重问题。本轮未发现必须立即修复的功能错误或安全边界问题。

#### 🟡 建议修改
**问题1：`__init__.py` 仍是主业务上帝类**
- 位置：`plugins.v2/subtitlemanualupload/__init__.py:80`
- 问题：主类同时承担插件生命周期、API 注册、配置归一化、本地媒体索引、字幕包解压、字幕匹配、写盘、调轴任务、在线 AI、自动入库队列、匹配历史等职责。按方法聚合看，`api` 约 1025 行、`auto_transfer` 约 1005 行、`subtitle_target` 约 1034 行、`plugin_framework` 约 824 行仍压在同一类内。
- 建议：优先拆出 `auto_transfer.py`、`subtitle_package.py`、`subtitle_matcher.py`、`subtitle_writer.py`、`media_repository.py`，主类保留 MoviePilot 插件适配和 API 薄代理。

**问题2：后端 `get_form()` 与前端 `Config.vue` 形成配置默认值双事实源**
- 位置：`plugins.v2/subtitlemanualupload/__init__.py:507`、`plugins.v2/subtitlemanualupload/src/components/Config.vue:16`
- 问题：`get_form()` 510 行，且和 Vue 配置页维护同一批默认值，如在线源、调轴、自动入库、RAR 工具、OpenSubtitles 配置等。字段新增或迁移时容易出现前后端默认值不一致。
- 建议：拆出 `config.py` 放后端配置常量、归一化和迁移；`get_form()` 尽量只返回最小兼容表单或引用同一份默认配置。前端继续保留兜底默认值，但以 `initialConfig` 为主。

**问题3：自动入库链路已经具备独立服务边界**
- 位置：`plugins.v2/subtitlemanualupload/__init__.py:1446`、`plugins.v2/subtitlemanualupload/__init__.py:4464`、`plugins.v2/subtitlemanualupload/__init__.py:5193`
- 问题：自动入库队列、限流、整季包缓存、中文资源跳过、在线搜索、写盘、AI 兜底混在主类里，后续维护最容易互相牵连。
- 建议：拆成 `AutoTransferService`，由主插件注入搜索服务、写盘服务、AI 客户端、媒体上下文构造器。先搬纯队列与任务状态，再搬搜索/写入策略。

**问题4：字幕包处理、语言识别、目标匹配可以进一步独立测试**
- 位置：`plugins.v2/subtitlemanualupload/__init__.py:1815`、`plugins.v2/subtitlemanualupload/__init__.py:3693`、`plugins.v2/subtitlemanualupload/__init__.py:3958`
- 问题：RAR/7Z/ZIP 解包、语言后缀识别、集数 hint、自动补 target、候选选择是核心匹配能力，但仍作为主类私有方法存在。
- 建议：拆出 `subtitle_package.py` 和 `subtitle_matcher.py`，输入输出使用普通 dict 或轻量 dataclass，保持不依赖 MoviePilot 插件实例。

**问题5：在线外语字幕提交 AI 与调轴/写盘/AI 客户端耦合较深**
- 位置：`plugins.v2/subtitlemanualupload/__init__.py:5964`、`plugins.v2/subtitlemanualupload/__init__.py:6081`
- 问题：在线字幕下载、解包、ASS/SSA 转 SRT、调轴任务更新、AI subtitle override 构造和提交都在主类里；这块是高价值功能，也最需要独立回归保护。
- 建议：拆出 `online_ai.py` 或并入 `autosub_client.py` + `subtitle_writer.py`，主类 API 只做请求校验和调用。

#### 🟢 可选优化
**问题6：`online_subtitles/common.py` 已经成为在线模块内的新聚合大文件**
- 位置：`plugins.v2/subtitlemanualupload/online_subtitles/common.py:1`
- 建议：后续可拆为 `models.py`、`client.py`、`keywords.py`、`relevance.py`、`network.py`。当前 provider 架构已经比主类清晰，不必第一轮处理。

**问题7：`AppPage.vue` 也需要组件化**
- 位置：`plugins.v2/subtitlemanualupload/src/components/AppPage.vue:1`
- 建议：后端第一轮拆完后，再拆前端的媒体搜索、详情目标表、在线搜索弹窗、AI 任务弹窗、自动队列弹窗、匹配历史为独立组件或 composable。

### 亮点
- 在线字幕 provider 已拆出 `online_subtitles/providers/*`，站点差异没有继续污染主类。
- 调轴核心算法已在 `timeline_fixer.py` 独立，测试文件也覆盖较多边界。
- 现有测试已经覆盖在线字幕、缓存、自动入库、调轴、AI 联动等关键场景，适合支撑渐进式拆分。

### 结论
- 结论：`Approved`
- 严重问题：0 个
- 建议修改：5 个
- 可选优化：2 个
- 下一步建议：先拆后端自动入库、字幕包处理、匹配/写盘三块；每拆一块只做搬迁和补测试，不改行为。

---

## 2026-06-29 13:52

## 代码审查报告

### 审查目标
- 类型：线上实际安装代码审查 + 本地原版基线对比
- 范围：`10.10.10.189` 的 `moviepilot-v2` 容器，实际插件路径 `/app/app/plugins/subtitlemanualupload`
- 审查时间：2026-06-29 13:52

### 审查输入证据
- 实际运行容器：`moviepilot-v2`，镜像 `jxxghp/moviepilot-v2:latest`，状态 healthy
- 实际插件版本：`plugin_version = "0.1.71"`
- 实际代码一致性：远端 `/app/app/plugins/subtitlemanualupload` 与本地 `plugins.v2/subtitlemanualupload` 生成 hash 清单后完全一致，文件数均为 53，missing/extra/changed 均为空
- 自动化检查：容器内 `python -m py_compile` 覆盖主入口、兼容层、AI 联动、自动入库、在线 AI、写盘、目标解析、上传会话、调轴模块，通过
- 本地回归检查：`pytest tests/test_subtitlemanualupload_cache.py tests/test_subtitlemanualupload_online.py tests/test_subtitlemanualupload_tongwen.py tests/test_timeline_fixer.py tests/test_autosubv3_cancel.py` 通过，211 passed，1 skipped
- API 合同：拆分前基线 `134d40a` 到当前版本，插件 API 路由数量 23 -> 23，无新增、删除、变更路由
- 运行日志：`2026-06-29 13:18` 安装/重载后恢复本地资源缓存和匹配历史缓存成功；13:21、13:22、13:24 本地资源搜索正常；安装后窗口未发现 `Traceback`、`ERROR`、`ImportError`、`ModuleNotFoundError`
- AI 联动插件：`AutoSubv3` 版本 `3.5.54`，存在 `_status_payload`、`submit_tasks`、`cancel_tasks`、`restart_tasks`；`SubtitleManualUpload` 的 `autosub_bridge.py` 调用契约匹配
- AI 联动日志：`2026-06-29 11:00` 由字幕匹配提交 `百万英镑` AI 任务成功，AutoSubv3 完成 ASR、英文字幕生成和中英双语翻译落盘

### 原版功能面梳理
- 插件入口：启用状态、侧边栏入口、Vue federation 页面、配置表单。
- 本地媒体：资源缓存恢复/刷新、分页搜索、MoviePilot 整理历史目标解析、电影/剧集/季度目标分组、远程挂载信任模式。
- 手动上传：单字幕、ZIP、RAR、7Z 解包；压缩包资源限制；语言识别；自动匹配目标；预览确认；按媒体文件名写入。
- 字幕写入：目标文件名生成、同目录字幕清理、`.default`/`.forced` 标记处理、备份、恢复、删除单字幕、批量清空。
- 匹配历史：持久化缓存、文件系统签名校验、分页/筛选、媒体/季/集/字幕层级、历史字幕删除和调轴入口。
- 智能调轴：依赖检查、音频/内嵌字幕 VAD、缓存、最大/最小偏移、低可信拒绝、备份与恢复。
- 在线字幕：ASSRT、OpenSubtitles、SubHD、Zimuku provider；手动搜索链接；全源搜索；单源搜索；下载预览；在线字幕写入或提交 AI。
- 入库自动字幕：TransferComplete 事件监听、队列去重、整季包复用、每源限速、中文媒体/已有中文字幕跳过、在线优先或 AI 兜底策略。
- AI 字幕联动：状态读取、任务提交、任务查询、取消、重新生成、选中外挂字幕作为来源、在线外语字幕调轴后提交 AI。

### 问题清单

#### 🔴 严重问题
无。实际安装代码与本地 `0.1.71` 完全一致，API 合同未变，容器内编译通过，安装后日志无导入/运行级异常。

#### 🟡 建议修改
**问题1：线上日志里存在历史在线源限流和低可信调轴拒绝**
- 位置：运行环境日志 `/config/logs/plugins/subtitlemanualupload.log`
- 问题：历史窗口里 ASSRT 返回 HTTP 509，OpenSubtitles 返回每日下载额度限制，部分在线字幕因智能调轴低可信被拒绝写入或拒绝提交 AI。
- 风险：这是站点额度/候选质量/调轴安全门禁导致的预期拒绝，不是拆分回归；但用户使用时会表现为某些自动在线匹配失败后转 AI 或跳过。
- 建议：保留当前拒绝策略；如果想提升命中率，后续应优化 provider 排序、额度提示和候选过滤，而不是放宽低可信调轴门禁。

#### 🟢 可选优化
**问题2：Chrome 页面级点击未执行**
- 位置：当前 Codex 工具会话
- 问题：本轮明确读取了 Chrome 技能，但当前可用工具列表没有 Chrome 技能要求的浏览器控制入口，因此没有做登录态页面点击。
- 建议：本轮以容器代码 hash、运行日志和插件契约作为主要证据；如后续提供可用 Chrome 控制入口，可补做页面按钮级冒烟。

### 亮点
- `0.1.71` 实际安装版本已确认，且不是只改版本号：代码 hash 与本地包逐文件一致。
- 拆分后保留原 23 个 API 路由，主入口只收缩职责，未改变外部接口。
- AI 联动在真实环境有成功日志：字幕匹配提交任务，AutoSubv3 完成生成和翻译。

### 结论
- 结论：`Approved`
- 严重问题：0 个
- 建议修改：1 个
- 可选优化：1 个
- 下一步建议：可以把 `0.1.71` 作为当前可安装测试版继续试用；重点观察在线源额度和低可信调轴拒绝是否符合预期。

---

## 2026-06-29 13:41

## 代码审查报告

### 审查目标
- 类型：本地拆分后代码 + 线上 MoviePilot 安装环境核查
- 范围：`plugins.v2/subtitlemanualupload`，重点覆盖原版功能合同、插件商店版本、AI 字幕生成联动、线上安装路径
- 审查时间：2026-06-29 13:41

### 审查输入证据
- Diff 范围：拆分前基线 `134d40a` 到当前工作区
- 自动化检查：`pytest tests/test_subtitlemanualupload_cache.py tests/test_subtitlemanualupload_online.py tests/test_subtitlemanualupload_tongwen.py tests/test_timeline_fixer.py tests/test_autosubv3_cancel.py` 通过，211 passed，1 skipped
- 结构对比：`SubtitleManualUpload.get_api()` 路由数量 23 -> 23，路由 path、methods、summary 未变
- 线上环境：`https://mp.jaysherlock.top:5443/api/v1/plugin/SubtitleManualUpload/status` 返回 401 `Not authenticated`，证明匿名接口受鉴权保护；`mp.jaysherlock.top` 解析到 `10.10.10.1`
- NAS 检查：`root@10.10.10.143` 可连，但 `docker ps` 未发现运行中的 MoviePilot 容器；`/home/jay/moviepilot-test` 的 compose 项目当前无运行服务；本地配置里的 `config/plugins/SubtitleManualUpload` 仅有 `library_index.json`

### 问题清单

#### 🔴 严重问题
无基于本地代码和测试可证实的严重回归。

#### 🟡 建议修改
**问题1：线上“实际已安装最新版”的环境证据不闭环**
- 位置：环境部署，不是源码文件
- 问题：用户给出的 URL 实际解析到 `10.10.10.1`，但可 SSH 的 `10.10.10.143` 没有运行中的 MoviePilot 容器，也没有 `0.1.71` 插件代码。`10.10.10.143` 上能找到的备份/本地仓库版本分别是 `0.1.19` 和 `0.1.0`，不是最新版。
- 风险：无法证明浏览器里正在运行的 MP 就是本地打包的 `0.1.71`。
- 建议：用已登录 Chrome 或实际运行主机的容器 shell 再确认插件状态页/插件文件版本。

#### 🟢 可选优化
**问题2：拆分后的兼容层仍承担大量旧私有方法转发**
- 位置：`plugins.v2/subtitlemanualupload/compat.py`
- 建议：当前作为迁移兼容层是合理的；后续可以逐步把测试依赖从旧私有方法迁到服务对象，减少兼容层体积。

### 亮点
- 原版功能入口未丢失：23 个插件 API 路由与拆分前基线一致。
- 主类从 231 个方法收缩到 50 个方法，迁移到配置、上传会话、目标解析、历史、写盘、AI 联动、自动入库等服务模块。
- AI 字幕生成联动仍保留状态读取、提交、查询、取消、重新生成、选中外挂字幕 override、在线字幕提交 AI 的入口，并有 `tests/test_autosubv3_cancel.py` 回归覆盖。

### 结论
- 结论：`Approved with environment caveat`
- 严重问题：0 个
- 建议修改：1 个
- 可选优化：1 个
- 下一步建议：先确认实际线上运行主机或提供 Chrome 登录态后，再做页面级安装验证。

---

## 2026-06-29 14:40

## Chrome CDP 实测补充

### 实测目标
- 类型：实际 Chrome 页面上下文 + 线上 MoviePilot 容器联动验证
- URL：`https://mp.jaysherlock.top:5443/`
- 实际 NAS：`10.10.10.189`
- 容器：`moviepilot-v2`
- 插件版本：`SubtitleManualUpload 0.1.71`

### 实测输入证据
- Chrome CDP：`127.0.0.1:39222`，当前 MP 页面停留在登录页 `#/login`。
- 鉴权方式：在页面上下文使用 `fetch` + `X-API-KEY` 调用插件 API；`.env` 中 `API_TOKEN` 带外层引号，脚本需按 dotenv 语义去掉外层引号后再发起请求。
- 容器代码：`/app/app/plugins/subtitlemanualupload` 与本地 `plugins.v2/subtitlemanualupload` 逐文件 hash 一致，文件数均为 53，无 missing/extra/changed。
- 编译检查：容器内插件 Python 文件 `py_compile` 通过。

### CDP 接口实测结果
- `GET /api/v1/plugin/SubtitleManualUpload/status`：`200 OK`
  - `enabled=true`
  - `auto_search_on_transfer=true`
  - `auto_transfer_subtitle_strategy=online_then_ai_source`
  - `traditional_to_simplified=true`
  - `ai_subtitle.available=true`
  - `ai_subtitle.running=true`
  - `ai_subtitle.queue_ready=true`
  - `ai_subtitle.plugin_version=3.5.54`
- `GET /api/v1/plugin/SubtitleManualUpload/online_status`：`200 OK`
  - 可用在线源：`SubHD`、`Zimuku`、`射手网(伪)`、`OpenSubtitles`
- `GET /api/v1/plugin/SubtitleManualUpload/auto_transfer_queue?limit=5`：`200 OK`
  - `pending=0`
  - `in_progress=0`
  - `completed=0`
  - `skipped=0`
  - `failed=0`
  - `active=0`
  - `total=0`
  - `worker_alive=false`
- `POST /api/v1/plugin/SubtitleManualUpload/ai_tasks`，body 为 `{"target_ids":[]}`：`200 OK`
  - `success=true`
  - `message=ok`
  - 未触发实际 AI 任务。
- `GET /api/v1/plugin/SubtitleManualUpload/search?page=1&page_size=5&media_type=all`：`200 OK`
  - `total=683`
  - 返回 5 条媒体；首条为《我与超人的冒险》`2023`，`media_type=tv`，含 `tmdb_id`。
- `GET /api/v1/plugin/SubtitleManualUpload/targets?...`：`200 OK`
  - 针对《我与超人的冒险》返回 40 个本地目标。
- `GET /api/v1/plugin/SubtitleManualUpload/match_history?page=1&page_size=5&media_type=all`：`200 OK`
  - `total=129`
  - 返回 5 条匹配历史。

### AI 字幕联动核查
- `status` 明确显示 AutoSubv3 可用、运行中、队列可用，版本为 `3.5.54`。
- 插件侧 `ai_tasks` 接口空目标冒烟成功，说明 SubtitleManualUpload 到 AI 字幕插件的状态读取和提交入口可达。
- 运行日志中已有实际联动证据：SubtitleManualUpload 曾提交 AI 字幕任务，AutoSubv3 完成 ASR、英文字幕生成和中英双语翻译落盘。

### 发现与判断
- 未发现拆分导致的功能缺失或 API 合同回归。
- 之前 CDP 调用出现 `401 apikey 校验不通过` 的根因是测试脚本把 `.env` 的外层引号当作 token 内容发送，不是插件 bug。
- 探索过程中出现的 `/online/status`、`/ai/tasks`、`remoteEntry.js` 等 `404` 来自错误路径或前端探测路径，不属于当前 `SubtitleManualUpload.get_api()` 暴露的 23 个原版插件 API。
- 当前 Chrome 没有登录态，因此本轮没有做插件页面按钮点击；但已在真实 Chrome 页面上下文完成核心插件 API 和联动状态实测。

### 结论
- 结论：`Approved`
- 严重问题：0 个
- 建议修改：0 个
- 残余风险：未触发真实下载/写盘/AI 生成任务；本轮只做只读接口与空 AI 任务提交冒烟，避免影响线上媒体库。

---
